<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.crowdfund.domain.category.mapper.CategoryMapper">

    <insert id="insert">
        INSERT INTO category (name, parent_id, depth, sort_order, is_active)
        VALUES (#{category.name}, #{category.parentId}, #{depth}, #{sortOrder}, 1)
    </insert>

    <update id="updateName">
        UPDATE category SET name = #{name} WHERE id = #{id}
    </update>

    <update id="updateSortOrder">
        UPDATE category SET sort_order = #{sortOrder} WHERE id = #{id}
    </update>

    <update id="delete">
        UPDATE category SET is_active = 0 WHERE id = #{id}
    </update>

    <update id="updateParentId">
        UPDATE category SET parent_id = #{parentId}, depth = #{depth} WHERE id = #{id}
    </update>

</mapper>