<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.authservice.crowdfund.domain.user.mapper.UserMapper">

    <update id="updateUserData">
        UPDATE user
        SET nickname = #{request.nickname},
            name = #{request.name},
            phone = #{request.phone}
        WHERE id = #{userId}
    </update>
    <update id="deactivateUser">
        UPDATE user
        SET deleted_at = NOW()
        WHERE id = #{userId}
          AND deleted_at IS NULL
    </update>

</mapper>
